# Реализовать экспорт CSV транзакций

Работай только над шагом 18 `ARCHITECTURE.md`: реализуй streaming CSV export транзакций с теми же фильтрами, разрешенными колонками, корректным quoting и защитой от spreadsheet formulas. HTTP tests: headers, CSV content и изоляция текущего пользователя. Полностью заверши отдельную задачу согласно `AGENTS.md`: изучи документы (включая database, api contracts, testing, security), код и local agents. Не меняй публичные ранее существующие контракты, кроме нового endpoint из плана. В конце: релевантные тесты, `./gradlew verify`, независимый code review с исправлением critical findings, prompt в `promts`, следующая сквозная запись `REPORT`, проверка git status/diff/log, один коммит в `develop` только файлов шага. Сообщи описание, команды/результаты, commit hash и измененные файлы.
