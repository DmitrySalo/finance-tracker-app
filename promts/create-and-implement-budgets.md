Fresh separate developer session for ARCHITECTURE.md plan step 14 only, current develop after steps 9-13. Read mandatory architecture/java/testing/database/api/security docs and applicable skills plus closest AGENTS before edits. Implement budgets table via new Flyway migration, JPA/domain/application/API CRUD under existing patterns, unique category/month, only EXPENSE categories, ownership/non-disclosure, optimistic versioning/client version conflict behavior, and audit create/update/delete in same DB transaction using existing audit design (extend safely for budget snapshots/actions). Month invariant must be first day, limit positive, values appropriate as architecture. Add unit/integration/HTTP tests for all stated invariants, ownership, version conflicts, and audit logs. Do not implement budget calculation (step 15) beyond data needed for it. Execute focused tests and ./gradlew verify. Conduct self-review and independent review. Then fully close this plan step per AGENTS: add original task wording to promts/<commit-name>.md, append correctly numbered REPORT.md step, inspect git status/diff/log, stage only task files and commit to develop. Preserve unrelated untracked .opencode/skills/frontend-code-review/. Return concise commit/files/test commands/results/review/blockers only.
