### 1

1. Реализуй шаги начиная с 9 и заканчивая 15 из Плана разработки ARCHITECTURE.md.
2. Каждый шаг реализуй в рамках отдельной подзадачи.
3. Каждая подзадача выполняется в рамках отдельной сессии.
4. Каждая подзадача (шаг из плана разработки) выполняется последовательно согласно указанному порядку и завершается запуском тестов, записью в REPORT.md и правильно оформленным коммитом, согласно установленным правилам в AGENTS.md.
5. Даю согласие обращаться в директории, находящиеся за пределами проекта, если это необходимо.
6. Если реализация какой-то подзадачи (шага из плана разработки) конфликтует с реализацией другой подзадачи (шага из плана разработки) или конфликтует с правилами, установленными при работе с проектом, то выбери наиболее оптимальный компромиссный вариант и продолжи работу над задачей.

### 2

1. Реализуй шаги начиная с 16 и заканчивая 31 из Плана разработки ARCHITECTURE.md.
2. Каждый шаг реализуй в рамках отдельной подзадачи.
3. Каждая подзадача выполняется в рамках отдельной сессии.
4. Каждая подзадача (шаг из плана разработки) выполняется последовательно согласно указанному порядку и завершается запуском тестов, записью в REPORT.md и правильно оформленным коммитом, согласно установленным правилам в AGENTS.md.
5. Даю согласие обращаться в директории, находящиеся за пределами проекта, если это необходимо.
6. Если реализация какой-то подзадачи (шага из плана разработки) конфликтует с реализацией другой подзадачи (шага из плана разработки) или конфликтует с правилами, установленными при работе с проектом, то выбери наиболее оптимальный компромиссный вариант и продолжи работу над задачей.