# Шаг 22. Инициализировать frontend

Выполни только шаг 22 `ARCHITECTURE.md` в отдельной сессии: создай `frontend/` на Vite, React 19, TypeScript и ESLint, npm scripts, API proxy и Docker-compatible production build. Итог: `npm run lint`, `npm run test`, `npm run build` проходят. Соблюдай `AGENTS`, сначала прочти архитектуру, frontend-related instructions/skills, testing and security docs, и исследуй current repo. Не делай foundation/functional UI из следующих шагов. Не добавляй Dockerfile (это шаг 31), но production build должен быть совместим с будущей контейнеризацией. После implementation execute exact relevant npm commands and `./gradlew verify` if required by root instruction (do so); perform independent frontend code review and fix critical issues. Then promts prompt with source task, next REPORT record, inspect git status/diff/log and make a single scoped commit to develop. Return commit hash, files, commands/results.
