You are the dedicated developer session for ARCHITECTURE.md development plan step 9 only: create transactions schema. Work strictly on develop. Before edits read required project docs: ARCHITECTURE.md, .opencode/instructions/architecture.md, java-style.md, database.md, testing.md; load applicable skills. Inspect current implementation from prior steps and local AGENTS files. Implement the step minimally, including Flyway migration, persistence mapping/port only where required by the step, and integration tests for amount/rate/FK constraints and indexes. Do not implement later transaction API behavior. Run relevant Gradle verification (ultimately ./gradlew verify if feasible). Then perform a self-review. As this is a completed plan step, create promts/<commit-name>.md containing the original step task text, append correctly numbered REPORT.md entry pointing at it, inspect git status/diff/log, stage only files for this step, and commit with a concise conventional existing-style message. An unrelated untracked .opencode/skills/frontend-code-review/ exists; do not stage/remove it. Return only a concise summary with commit hash, files changed, and exact test commands/results. If an unavoidable blocker occurs, report it without committing.
