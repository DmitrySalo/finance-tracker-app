Fresh dedicated developer session, implement ARCHITECTURE.md plan step 13 ONLY, from current develop after step 12. First read all mandatory instructions and local AGENTS; include database/testing/security considerations and appropriate skills. Inspect prior API/persistence patterns. Add audit_logs Flyway schema and application-level immutable audit record that stores required safe business before/after states (do not expose/log secrets) for transaction create/update/delete in the same transaction. Use injected Clock/current actor as appropriate. Add integration tests validating actor, UTC time, action, before/after state and audit rollback when enclosing operation fails. Ensure state is transactionally coupled to actual transaction mutation. Do NOT add audit API (later) or budgets (step 14). Run exact focused tests and ./gradlew verify, self-review and independent review. Then according to AGENTS complete plan-step delivery: create promts/<commit-name>.md containing original user-step wording, append correctly sequential REPORT.md entry, inspect git status/diff/log, stage only current work and commit on develop. Do not modify/stage unknown unrelated untracked .opencode/skills/frontend-code-review/. Return only concise summary: commit, changed scope, test commands/results, review, blockers.
