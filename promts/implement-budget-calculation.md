# Реализовать расчет бюджета

Fresh independent developer session continuing ONLY ARCHITECTURE.md plan step 15 after a previous fresh session identified an underspecification. User explicitly directs selecting an optimal compromise and continuing. Chosen minimal deterministic resolution: a budget's currency is constrained to its owner's base currency, making the budget exchange rate exactly 1.0. This is necessary because budgets lack a stored rate but transaction exchangeRateToBase is immutable; it allows reliable aggregation conversion. Before editing read all AGENTS and mandatory architecture/java/testing/database/api/security docs plus relevant skills; inspect current code and tests. Implement this resolution minimally and document the architecture-significant decision in ARCHITECTURE.md because it refines stated budget behavior. Migration must safely enforce the invariant against users.base_currency without unsupported cross-table check: choose a PostgreSQL-safe strategy compatible with existing Flyway history and clean DB, such as a composite FK supported by unique pair and matching columns, or enforce explicitly at service level plus justified DB integrity approach. Preserve public compatibility where feasible; probably validation returns existing standard 400. Then implement budget calculation: bounded PostgreSQL aggregation of EXPENSE transactions for category/month, converting amount * transaction.exchange_rate_to_base since budget is base currency; response includes spent, limit, remaining, percentage. Use only persisted transaction rate, no external rate. Adapt endpoint DTO only as needed, include BigDecimal semantics/scale. Add focused unit edge tests (0%, 100%, exceed, different transaction currencies) and PostgreSQL integration aggregation test. Do not add later dashboard. Run focused suite and ./gradlew verify, perform independent review. Fully close plan step per AGENTS: prompt original wording (may explain resolved conflict), REPORT next sequential correct entry, inspect status/diff/log, stage only task files and commit to develop. Preserve unrelated untracked .opencode/skills/frontend-code-review/. Return concise Russian summary: resolution, commit/files, exact tests/results, review/blockers.
