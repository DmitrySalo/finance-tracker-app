# Шаг 27. Реализовать CSV UI

В отдельной сессии сделать только шаг 27 `ARCHITECTURE.md`: CSV UI. Добавить export по текущим transaction filters, upload, column mapping, preview rows и line errors до confirmation. Component tests должны покрывать mapping, disabled confirm при ошибках и CSV upload. Следовать `AGENTS.md`, skills/docs frontend, testing, security и API contracts. Использовать существующие backend endpoints/contracts и UI filters транзакций. Не затрагивать budgets/dashboard. Завершить `npm run lint`, `npm run test`, `npm run build` и `./gradlew verify`; провести независимое frontend review, устранить critical findings; создать prompt в `promts`, следующую запись `REPORT.md`, проверить `git status`/`git diff`/`git log` и сделать ровно один scoped commit в `develop`. Вернуть точные проверки, файлы и hash commit.
