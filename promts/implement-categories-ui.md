В отдельной сессии реализуй только шаг 25 ARCHITECTURE.md: UI категорий (список, create/edit form, color/icon selector, delete confirmation). Component tests должны проверить optimistic invalidation и отображение server validation/conflict error. Соблюдай AGENTS and relevant frontend/testing/security instructions and skills; study existing API/foundation/auth patterns. Keep scope only categories UI. At end npm lint/test/build and ./gradlew verify; independent frontend code review, fix critical issues; prompt file, REPORT next record, git checks and exactly one scoped commit develop. Return commit summary, hash, files, exact test results.
